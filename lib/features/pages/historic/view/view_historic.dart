import 'package:flutter/material.dart';

class ViewHistoric extends StatelessWidget {
  const ViewHistoric({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const Text('Teste 2'));
  }
}
